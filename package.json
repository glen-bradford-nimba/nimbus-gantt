{
  "name": "nimbus-gantt-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/core",
    "dev": "npm run dev -w packages/demo",
    "test": "npm run test -w packages/core",
    "lint": "eslint packages/*/src --ext .ts,.tsx"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "@types/three": "^0.183.1",
    "three": "^0.183.2",
    "ws": "^8.20.0"
  }
}
